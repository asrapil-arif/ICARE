@using I_Care.Models;
@{
    ViewBag.Title = "Master Data";
    ViewBag.Parent = "Master Data";
    ViewBag.Child = "";
    ViewBag.Icon = "<i class=\"ace-icon fa fa-database\"></i>";


    var ROLES = new I_Care.Classes.UserRoleProvider();
    string[] SSO_ALLOW = ROLES.GetRolesForUser(User.Identity.Name.ToString());
   
    FMSEntities db = new FMSEntities();
    var Item = (from m in db.m_item where m.ItemGroup == 1 select m);
    int H_W = Request.Browser.ScreenPixelsHeight + 24;

}



<style>
    .my-icon {
        border: 1px solid #dedede;
        border-radius: 5px;
        width: 120px;
        text-align: center;
        padding: 5px;
        cursor: pointer;
        display: inline-table;
        margin: 5px;
        transition: 0.1s;
    }

        .my-icon img {
            width: 70px;
        }

        .my-icon:hover {
            opacity: .80;
            -webkit-filter: grayscale(0); /* Safari 6.0 - 9.0 */
            filter: grayscale(0);
        }

            .my-icon:hover > img {
                -webkit-filter: grayscale(0); /* Safari 6.0 - 9.0 */
                filter: grayscale(0);
            }

    #icon-content {
        margin: 10px;
    }
</style>


<div style="margin:10px;" id="home-page">

    <div style="font-size:16px;color:#5D8AA5;font-weight:bold;text-decoration:none;margin:15px;">
        Manage Master Data
    </div>

    <div id="icon-content">





        <div class="my-icon" onclick="to_menu('/MasterData/MasterItem')">
            <img src="../../Content/img/icon/m_data_item.png" id="welcome-img">
            <br />
            Item
        </div>



        <div class="my-icon" onclick="to_menu('/MasterData/MasterPic')">
            <img src="../../Content/img/icon/pic.png" id="welcome-img">
            <br />
            Pic Process
        </div>



    </div>


    <script>

    function to_menu(cotroller) {
        var URI = URL_PTR + cotroller;
        window.location = URI;
    }

    </script>
